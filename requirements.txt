# Dinner Recipe Maker - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
streamlit>=1.28.0
openai>=1.0.0
supabase>=2.0.0
Pillow>=10.0.0

# Additional dependencies (likely already included with above)
python-dotenv>=1.0.0  # For environment variables (optional)
